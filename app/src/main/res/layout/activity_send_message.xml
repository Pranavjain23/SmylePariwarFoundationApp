<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.SendMessageActivity">

    <TextView
        android:textSize="20sp"
        android:text="hello"
        android:textColor="#000"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/txtMessageNumber"/>

    <EditText
        android:layout_below="@+id/txtMessageNumber"
        android:id="@+id/etMessageCode"
        android:layout_width="match_parent"
        android:layout_height="60dp"/>
    <EditText
        android:id="@+id/etMessageNumber"
        android:layout_below="@+id/etMessageCode"
        android:layout_width="match_parent"
        android:layout_height="60dp"/>

    <EditText
        android:layout_marginTop="40dp"

        android:layout_below="@+id/etMessageNumber"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:id="@+id/etMessageData"/>

    <Button
        android:layout_below="@+id/etMessageData"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:text="SEND"
        android:id="@+id/btnSend"/>

</RelativeLayout>